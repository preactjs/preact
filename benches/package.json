{
	"name": "preact-benchmarks",
	"private": true,
	"version": "1.0.0",
	"description": "Benchmarks for Preact",
	"scripts": {
		"start": "tach --config ./tach.config.json",
		"bench": "node ./scripts bench",
		"config": "node ./scripts config",
		"serve": "node ./scripts serve",
		"help": "node ./scripts --help"
	},
	"license": "MIT",
	"dependencies": {
		"afterframe": "^1.0.1"
	},
	"devDependencies": {
		"globby": "^11.0.0",
		"sade": "^1.7.3",
		"tachometer": "^0.4.17"
	}
}
