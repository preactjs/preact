/**
 * Global options
 * @public
 * @typedef Options
 * @property {(component: import('./internal').VNode) => void} [commitRoot] Hook invoked after a tree has been mounted or was updated.
 * @property {(component: import('./internal').VNode) => void} [beforeUnmount] Hook invoked immediately before a component is unmounted.
 */

/** @type {Options}  */
const options = {};

export default options;
